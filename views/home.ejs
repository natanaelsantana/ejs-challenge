<%- include('partials/header') -%>

    <h1>Home</h1>

    <%= homeStartingContent %>

    <div class="box">
        <% postItem.forEach(function(post) { %>
            
            
                <h1> <%= post.itemTitle %></h1>
                <p style="width: 50%; overflow: hidden; text-overflow: ellipsis"><%= post.item %></p>
                <a href="/post/<%= post.itemTitle %>">Read More</a>

                <% }) %>

    
    </div>
<%- include('partials/footer') -%>